{
  "name": "nodeexpress",
  "version": "1.0.0",
  "scripts": {
    "prestart": "npm install --no-audit --no-fund",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "json-server": "^0.17.3"
  }
}
